{
  "name": "playwright-grammar-test",
  "version": "1.0.0",
  "description": "Playwright framework to test grammar mistakes in webpages",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:specific": "playwright test tests/",
    "report": "playwright show-report"
  },
  "keywords": [
    "playwright",
    "testing",
    "grammar",
    "automation"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
